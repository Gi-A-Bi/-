-- =============================================================================
-- 학급 전체 경험치 보정치 (bonus_xp)
--   - "학급 전체 경험치" = 모든 학생 xp 합계 + bonus_xp
--   - 학급 단위 보상(+) / 차감(-) 시, 개별 학생의 xp 는 건드리지 않고
--     이 bonus_xp 값만 조정한다. (예: 학급 전체 파티 보상으로 -500)
--   - Supabase SQL Editor 에서 한 번 실행해 주세요. (여러 번 실행해도 안전)
-- =============================================================================
ALTER TABLE classes
  ADD COLUMN IF NOT EXISTS bonus_xp integer NOT NULL DEFAULT 0;
